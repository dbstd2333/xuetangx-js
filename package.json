{
  "name": "xuetangx-autolearn",
  "version": "1.8.0",
  "description": "学堂在线视频自动学习面板脚本",
  "type": "module",
  "scripts": {
    "dev": "node build.js --watch",
    "build": "node build.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
